---
phase: 13-demo-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Category 3 - IA West Smart Match CRM/frontend/src/components/FeedbackForm.tsx"
  - "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/LandingPage.tsx"
  - "Category 3 - IA West Smart Match CRM/frontend/src/app/components/ScrollToTop.tsx"
  - "Category 3 - IA West Smart Match CRM/frontend/src/app/components/Layout.tsx"
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-03

must_haves:
  truths:
    - "No 'Phase #N' text appears anywhere in the rendered app"
    - "LandingPage nav subtitle reads 'Coordinator Platform' instead of 'V1.2 public brand'"
    - "LandingPage hero pill reads 'IA West Chapter' instead of 'IA West V1.2 blue / white brand'"
    - "Navigating between coordinator routes resets scroll position to top"
  artifacts:
    - path: "Category 3 - IA West Smart Match CRM/frontend/src/components/FeedbackForm.tsx"
      provides: "Phase 12 badge removed"
      contains: "no 'Phase 12' string"
    - path: "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/LandingPage.tsx"
      provides: "Product-ready copy replacing dev-flavored strings"
      contains: "Coordinator Platform"
    - path: "Category 3 - IA West Smart Match CRM/frontend/src/app/components/ScrollToTop.tsx"
      provides: "Scroll-to-top on route change"
      exports: ["ScrollToTop"]
    - path: "Category 3 - IA West Smart Match CRM/frontend/src/app/components/Layout.tsx"
      provides: "ScrollToTop rendered inside router context"
      contains: "ScrollToTop"
  key_links:
    - from: "Category 3 - IA West Smart Match CRM/frontend/src/app/components/Layout.tsx"
      to: "Category 3 - IA West Smart Match CRM/frontend/src/app/components/ScrollToTop.tsx"
      via: "import and render"
      pattern: "import.*ScrollToTop"
---

<objective>
Remove all internal dev labels, replace dev-flavored copy with product-ready text, and add scroll-to-top behavior on route changes so the app is submission-ready.

Purpose: Phase 13 delivers a demo-polished frontend with zero internal references, professional copy, and smooth navigation.
Output: Four files modified/created — FeedbackForm cleanup, LandingPage copy, ScrollToTop component, Layout integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-demo-polish/13-RESEARCH.md

<interfaces>
<!-- Layout.tsx imports and structure (executor must place ScrollToTop here) -->
From Category 3 - IA West Smart Match CRM/frontend/src/app/components/Layout.tsx:
```tsx
import { Outlet, Link, useLocation } from "react-router";
// ... lucide imports ...
import { useState } from "react";

export function Layout() {
  const location = useLocation();
  const [sidebarOpen, setSidebarOpen] = useState(false);

  return (
    <div className="min-h-screen bg-background">
      {/* ScrollToTop goes here, as first child */}
```

From Category 3 - IA West Smart Match CRM/frontend/src/components/FeedbackForm.tsx (lines 146-148, to be deleted):
```tsx
<div className="rounded-full border border-blue-100 bg-blue-50 px-3 py-1 text-xs font-medium text-blue-700">
  Phase 12
</div>
```

From Category 3 - IA West Smart Match CRM/frontend/src/app/pages/LandingPage.tsx (lines 56, 72):
```tsx
// Line 56 — nav subtitle:
<p className="text-xs uppercase tracking-[0.28em] text-muted-foreground">V1.2 public brand</p>
// Line 72 — hero pill:
<span className="public-pill">IA West V1.2 blue / white brand</span>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Phase 12 badge from FeedbackForm (POLISH-01)</name>
  <files>Category 3 - IA West Smart Match CRM/frontend/src/components/FeedbackForm.tsx</files>
  <read_first>
    - Category 3 - IA West Smart Match CRM/frontend/src/components/FeedbackForm.tsx
  </read_first>
  <action>
Delete the entire 3-line Phase 12 badge element at lines 146-148 of FeedbackForm.tsx. The exact lines to remove are:

```tsx
<div className="rounded-full border border-blue-100 bg-blue-50 px-3 py-1 text-xs font-medium text-blue-700">
  Phase 12
</div>
```

Do NOT replace it with other text. The title and description props already provide sufficient context for the form. Just delete those 3 lines.

After deletion, run an exhaustive grep across ALL .tsx and .ts files in the frontend/src directory to confirm zero remaining "Phase" + number strings:

```bash
grep -rn "Phase [0-9]" "Category 3 - IA West Smart Match CRM/frontend/src" --include="*.tsx" --include="*.ts"
```

Expected result: zero output lines. If any other occurrences are found, remove them following the same pattern (delete the containing element, do not replace with other text).
  </action>
  <verify>
    <automated>cd "Category 3 - IA West Smart Match CRM/frontend" && grep -rn "Phase [0-9]" src/ --include="*.tsx" --include="*.ts" | wc -l | grep -q "^0$" && echo "PASS: No Phase #N strings found" || echo "FAIL: Phase #N strings still present"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "Phase [0-9]" across all src/**/*.tsx and src/**/*.ts returns zero matches
    - The FeedbackForm.tsx file no longer contains the string "Phase 12"
    - The FeedbackForm still renders its title, description, and form fields (no structural breakage)
  </acceptance_criteria>
  <done>Zero "Phase #N" strings exist in any .tsx or .ts file under frontend/src/.</done>
</task>

<task type="auto">
  <name>Task 2: Replace dev-flavored copy on LandingPage (POLISH-02)</name>
  <files>Category 3 - IA West Smart Match CRM/frontend/src/app/pages/LandingPage.tsx</files>
  <read_first>
    - Category 3 - IA West Smart Match CRM/frontend/src/app/pages/LandingPage.tsx
  </read_first>
  <action>
Make exactly two string replacements in LandingPage.tsx:

1. **Line 56 (nav subtitle):** Change the text content from `V1.2 public brand` to `Coordinator Platform`. The surrounding JSX stays identical:
   ```tsx
   <p className="text-xs uppercase tracking-[0.28em] text-muted-foreground">Coordinator Platform</p>
   ```

2. **Line 72 (hero pill):** Change the text content from `IA West V1.2 blue / white brand` to `IA West Chapter`. The surrounding JSX stays identical:
   ```tsx
   <span className="public-pill">IA West Chapter</span>
   ```

Do NOT change any other text on the page. The rest of the LandingPage copy (hero heading, hero subtitle, section titles, feature descriptions) is already product-ready per the research audit.
  </action>
  <verify>
    <automated>cd "Category 3 - IA West Smart Match CRM/frontend" && grep -c "V1\.2 public brand\|V1\.2 blue" src/app/pages/LandingPage.tsx | grep -q "^0$" && grep -q "Coordinator Platform" src/app/pages/LandingPage.tsx && grep -q "IA West Chapter" src/app/pages/LandingPage.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "V1.2 public brand" in LandingPage.tsx returns zero matches
    - grep "V1.2 blue" in LandingPage.tsx returns zero matches
    - grep "Coordinator Platform" in LandingPage.tsx returns exactly one match
    - grep "IA West Chapter" in LandingPage.tsx returns exactly one match
    - No other text on the page was modified
  </acceptance_criteria>
  <done>LandingPage contains zero dev-flavored version strings; nav subtitle reads "Coordinator Platform" and hero pill reads "IA West Chapter".</done>
</task>

<task type="auto">
  <name>Task 3: Add ScrollToTop component and wire into Layout (POLISH-03)</name>
  <files>
    Category 3 - IA West Smart Match CRM/frontend/src/app/components/ScrollToTop.tsx,
    Category 3 - IA West Smart Match CRM/frontend/src/app/components/Layout.tsx
  </files>
  <read_first>
    - Category 3 - IA West Smart Match CRM/frontend/src/app/components/Layout.tsx
    - Category 3 - IA West Smart Match CRM/frontend/src/styles/theme.css (confirm scroll-behavior: smooth exists — do NOT add it again)
  </read_first>
  <action>
**Step 1: Create ScrollToTop.tsx**

Create a new file at `Category 3 - IA West Smart Match CRM/frontend/src/app/components/ScrollToTop.tsx` with this exact content:

```tsx
import { useEffect } from "react";
import { useLocation } from "react-router";

export function ScrollToTop(): null {
  const { pathname } = useLocation();
  useEffect(() => {
    window.scrollTo(0, 0);
  }, [pathname]);
  return null;
}
```

**Step 2: Wire ScrollToTop into Layout.tsx**

In `Layout.tsx`, add this import at the top of the file (after existing imports):

```tsx
import { ScrollToTop } from "./ScrollToTop";
```

Then render `<ScrollToTop />` as the first child inside the root `<div>` of the Layout return statement. The Layout return currently starts with:

```tsx
return (
  <div className="min-h-screen bg-background">
```

Change it to:

```tsx
return (
  <div className="min-h-screen bg-background">
    <ScrollToTop />
```

**Important:** Do NOT add `scroll-behavior: smooth` to any CSS file. It already exists in `theme.css:155` and must not be duplicated.
  </action>
  <verify>
    <automated>cd "Category 3 - IA West Smart Match CRM/frontend" && test -f src/app/components/ScrollToTop.tsx && grep -q "import { ScrollToTop }" src/app/components/Layout.tsx && grep -q "useLocation" src/app/components/ScrollToTop.tsx && grep -q "window.scrollTo" src/app/components/ScrollToTop.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File src/app/components/ScrollToTop.tsx exists and exports a ScrollToTop function
    - ScrollToTop.tsx imports useLocation from "react-router" and useEffect from "react"
    - ScrollToTop.tsx calls window.scrollTo(0, 0) inside a useEffect depending on pathname
    - Layout.tsx imports ScrollToTop from "./ScrollToTop"
    - Layout.tsx renders <ScrollToTop /> as first child inside the root div
    - No new scroll-behavior CSS rule was added anywhere (theme.css already has it)
  </acceptance_criteria>
  <done>ScrollToTop component exists and is rendered inside Layout; all coordinator route changes reset scroll to top. No duplicate scroll-behavior CSS was added.</done>
</task>

</tasks>

<verification>
All three requirements verified by:

1. **POLISH-01:** `grep -rn "Phase [0-9]" "Category 3 - IA West Smart Match CRM/frontend/src" --include="*.tsx" --include="*.ts"` returns zero lines
2. **POLISH-02:** `grep -c "V1\.2" "Category 3 - IA West Smart Match CRM/frontend/src/app/pages/LandingPage.tsx"` returns 0; `grep "Coordinator Platform\|IA West Chapter" LandingPage.tsx` returns 2 matches
3. **POLISH-03:** ScrollToTop.tsx exists with useLocation + useEffect + window.scrollTo; Layout.tsx imports and renders it
</verification>

<success_criteria>
- Zero "Phase #N" strings in any frontend source file
- LandingPage nav subtitle is "Coordinator Platform", hero pill is "IA West Chapter"
- ScrollToTop component renders inside Layout and resets scroll on every route change
- No duplicate scroll-behavior CSS declarations added
- Frontend still builds without errors (existing build pipeline)
</success_criteria>

<output>
After completion, create `.planning/phases/13-demo-polish/13-01-SUMMARY.md`
</output>
