---
phase: 15-build-quality-playwright-evidence
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - "Category 3 - IA West Smart Match CRM/tests/test_react_e2e.py"
  - "Category 3 - IA West Smart Match CRM/scripts/run_react_e2e.py"
  - "Category 3 - IA West Smart Match CRM/output/playwright/react-qr-flow.png"
  - "Category 3 - IA West Smart Match CRM/output/playwright/react-feedback-flow.png"
autonomous: true
requirements: [VERIFY-01, VERIFY-02]

must_haves:
  truths:
    - "A Playwright test navigates to /outreach, generates a QR code, asserts the QR image renders, and captures a screenshot"
    - "A Playwright test navigates to /ai-matching, submits feedback via FeedbackForm, asserts weight-shift stats update, and captures a screenshot"
    - "Both screenshot PNGs exist in output/playwright/ as committed evidence"
  artifacts:
    - path: "Category 3 - IA West Smart Match CRM/tests/test_react_e2e.py"
      provides: "pytest-compatible Playwright E2E tests for QR and feedback flows"
      contains: "test_qr_flow"
    - path: "Category 3 - IA West Smart Match CRM/scripts/run_react_e2e.py"
      provides: "standalone runner script with auto-retry"
      contains: "sync_playwright"
    - path: "Category 3 - IA West Smart Match CRM/output/playwright/react-qr-flow.png"
      provides: "QR flow screenshot evidence"
    - path: "Category 3 - IA West Smart Match CRM/output/playwright/react-feedback-flow.png"
      provides: "Feedback flow screenshot evidence"
  key_links:
    - from: "tests/test_react_e2e.py"
      to: "http://localhost:5173/outreach"
      via: "Playwright page.goto + button clicks"
      pattern: "page\\.goto.*outreach"
    - from: "tests/test_react_e2e.py"
      to: "http://localhost:5173/ai-matching"
      via: "Playwright page.goto + form submission"
      pattern: "page\\.goto.*ai-matching"
    - from: "scripts/run_react_e2e.py"
      to: "tests/test_react_e2e.py"
      via: "imports and calls test functions"
      pattern: "from.*test_react_e2e.*import|test_qr_flow|test_feedback_flow"
---

<objective>
Write Python Playwright E2E tests for the QR code generation and feedback submission flows, run them to capture screenshot evidence, and commit the artifacts.

Purpose: VERIFY-01 and VERIFY-02 require browser-captured evidence proving the QR and feedback flows work end-to-end. Screenshots serve as hackathon submission proof.
Output: `tests/test_react_e2e.py` (pytest tests), `scripts/run_react_e2e.py` (standalone runner), and 2 PNG screenshots in `output/playwright/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-build-quality-playwright-evidence/15-CONTEXT.md

<interfaces>
<!-- Outreach page structure — selectors needed for QR flow test -->
From Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Outreach.tsx:
- Speaker dropdown: `<select>` with `value={selectedSpeaker}` — label text "Recipient"
- Event dropdown: `<select>` with `value={selectedEvent}` — label text "Event"
- Auto-selects first speaker and first event on page load via useEffect
- QR generation: `handleGenerateQr()` called by QRCodeCard's `onPrimaryAction`
- QRCodeCard button text: "Generate QR" (when no asset) or "Refresh QR" (when asset exists)
- QR image renders as `<img>` with alt text `"{speaker_name} referral QR"` inside QRCodeCard
- Referral code text appears in a `<p>` with class "font-semibold text-slate-900" under "Referral code" label

From Category 3 - IA West Smart Match CRM/frontend/src/components/QRCodeCard.tsx:
- Primary button: `<button>` containing text "Generate QR" or "Refresh QR"
- QR image: `<img src={previewSource} alt="{speaker_name} referral QR">`
- Referral code display: `<p>` containing `asset.referral_code` text

<!-- AIMatching page structure — selectors needed for feedback flow test -->
From Category 3 - IA West Smart Match CRM/frontend/src/app/pages/AIMatching.tsx:
- Event dropdown auto-selects first event on load
- Match cards render after ranking API call
- "Record Feedback" button on each match card: `<button>` with text "Record Feedback"
- Opens Dialog containing FeedbackForm component
- Feedback stats section shows: "Acceptance rate", "Pain score", "Membership signal", "Lead weight shift"
- Stats badge: `<span>` with text "{N} feedback rows"

From Category 3 - IA West Smart Match CRM/frontend/src/components/FeedbackForm.tsx:
- Decision buttons: "Accept" and "Decline" — default is "accept"
- "Submit Feedback" button: `<button>` with text "Submit Feedback"
- Success message: `<div>` containing "Feedback recorded" text
- Coordinator rating dropdown defaults to "4"

<!-- Existing runner pattern to follow -->
From Category 3 - IA West Smart Match CRM/scripts/run_playwright_demo_qa.py:
- Uses `from playwright.sync_api import sync_playwright`
- `snap(page, filename)` helper: waits 1.5s, screenshots to OUTPUT_DIR
- `wait_for_body_contains(page, substrings, timeout_ms)` helper: polls body text
- `attach_listeners(page)` for console/error tracking
- Headless Chromium with viewport 1440x1600
- OUTPUT_DIR = ROOT / "output" / "playwright"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_react_e2e.py with QR and feedback flow tests</name>
  <files>Category 3 - IA West Smart Match CRM/tests/test_react_e2e.py</files>
  <read_first>
    - Category 3 - IA West Smart Match CRM/scripts/run_playwright_demo_qa.py
    - Category 3 - IA West Smart Match CRM/frontend/src/app/pages/Outreach.tsx
    - Category 3 - IA West Smart Match CRM/frontend/src/components/QRCodeCard.tsx
    - Category 3 - IA West Smart Match CRM/frontend/src/app/pages/AIMatching.tsx
    - Category 3 - IA West Smart Match CRM/frontend/src/components/FeedbackForm.tsx
  </read_first>
  <action>
Create `Category 3 - IA West Smart Match CRM/tests/test_react_e2e.py` — a pytest-compatible file with two test functions using `playwright.sync_api`. The file should be structured as follows:

**Module docstring:**
```
"""
Playwright E2E tests for React QR and Feedback flows.

Requires:
  - React dev server at http://localhost:5173 (or vite preview after build)
  - FastAPI backend at http://localhost:8000 (with demo.db fallback)
  - Start both: python start_fullstack.py  OR  ./start_fullstack.sh
"""
```

**Constants:**
```python
REACT_URL = "http://localhost:5173"
ROOT = Path(__file__).resolve().parents[1]
OUTPUT_DIR = ROOT / "output" / "playwright"
```

**Helper functions (reuse pattern from run_playwright_demo_qa.py):**

1. `snap(page, filename, full_page=True)` — mkdir OUTPUT_DIR, wait 1500ms, screenshot to OUTPUT_DIR/filename, return relative path string.

2. `wait_for_body_contains(page, substrings, timeout_ms=60_000)` — poll `page.locator("body").inner_text()` until all substrings found, raise AssertionError with last sample if timeout.

**Test function: `test_qr_flow()`**

Steps:
1. Launch Chromium headless, viewport 1440x1600.
2. `page.goto(f"{REACT_URL}/outreach", wait_until="networkidle", timeout=60_000)`
3. `wait_for_body_contains(page, ["Recipient", "Event", "Generate QR"])` — confirms page loaded with dropdowns and QR card.
4. The Outreach page auto-selects the first speaker and event via useEffect, so no manual dropdown selection needed.
5. Click the "Generate QR" button: `page.get_by_role("button", name="Generate QR").click()`
6. Wait for the QR image to appear: `page.wait_for_selector("img[alt*='referral QR']", timeout=30_000)`
7. Assert the QR image is visible: `assert page.locator("img[alt*='referral QR']").is_visible()`
8. Also assert "Referral code" text appears on the page: `wait_for_body_contains(page, ["Referral code"])`
9. Screenshot: `snap(page, "react-qr-flow.png")`
10. Close browser.

**Test function: `test_feedback_flow()`**

Steps:
1. Launch Chromium headless, viewport 1440x1600.
2. `page.goto(f"{REACT_URL}/ai-matching", wait_until="networkidle", timeout=60_000)`
3. `wait_for_body_contains(page, ["feedback rows", "Acceptance rate"])` — confirms page loaded with feedback stats section.
4. Wait for match cards to render. The page auto-selects the first event and ranks speakers. Wait for "Record Feedback" button: `page.wait_for_selector("button:has-text('Record Feedback')", timeout=30_000)`
5. Read the current feedback count from the stats badge: `old_count_text = page.locator("text=/\\d+ feedback rows/").inner_text()`. Extract the integer with regex.
6. Click the first "Record Feedback" button: `page.get_by_role("button", name="Record Feedback").first.click()`
7. Wait for FeedbackForm dialog: `wait_for_body_contains(page, ["Submit Feedback", "Capture Match Outcome"])`
8. The form defaults to "accept" decision and rating "4", so just click submit: `page.get_by_role("button", name="Submit Feedback").click()`
9. Wait for success confirmation: `page.wait_for_selector("text=/Feedback recorded/", timeout=30_000)`
10. Wait a moment for the dialog to close and stats to refresh: `page.wait_for_timeout(3000)`
11. Verify the feedback count increased: read new count from `page.locator("text=/\\d+ feedback rows/").inner_text()`, extract integer, assert `new_count >= old_count + 1`.
12. Also verify the stats section still renders (not broken): `wait_for_body_contains(page, ["Acceptance rate", "Pain score", "Lead weight shift"])`
13. Screenshot: `snap(page, "react-feedback-flow.png")`
14. Close browser.

Each test function should use a `with sync_playwright() as pw:` context manager and handle its own browser lifecycle. Wrap each test body in try/finally to ensure browser.close() runs even on assertion failure.

Use `from __future__ import annotations` at the top. Add type annotations on all functions per PEP 8 guidelines.
  </action>
  <verify>
    <automated>cd "Category 3 - IA West Smart Match CRM" && python -c "import ast; ast.parse(open('tests/test_react_e2e.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at "Category 3 - IA West Smart Match CRM/tests/test_react_e2e.py"
    - grep "def test_qr_flow" returns a match
    - grep "def test_feedback_flow" returns a match
    - grep "react-qr-flow.png" returns a match
    - grep "react-feedback-flow.png" returns a match
    - grep "sync_playwright" returns a match
    - grep "Generate QR" returns a match (button click)
    - grep "Record Feedback" returns a match (button click)
    - grep "Submit Feedback" returns a match (button click)
    - grep "referral QR" returns a match (img alt assertion)
    - grep "Feedback recorded" returns a match (success assertion)
    - grep "feedback rows" returns a match (count assertion)
    - python -c "import ast; ast.parse(...)" succeeds (valid syntax)
  </acceptance_criteria>
  <done>test_react_e2e.py contains two pytest-compatible test functions covering the QR generation flow and the feedback submission flow, with screenshot capture to output/playwright/.</done>
</task>

<task type="auto">
  <name>Task 2: Create run_react_e2e.py standalone runner and execute tests to capture evidence</name>
  <files>
    Category 3 - IA West Smart Match CRM/scripts/run_react_e2e.py
    Category 3 - IA West Smart Match CRM/output/playwright/react-qr-flow.png
    Category 3 - IA West Smart Match CRM/output/playwright/react-feedback-flow.png
  </files>
  <read_first>
    - Category 3 - IA West Smart Match CRM/tests/test_react_e2e.py
    - Category 3 - IA West Smart Match CRM/scripts/run_playwright_demo_qa.py
  </read_first>
  <action>
**Part A: Create `scripts/run_react_e2e.py`**

Create `Category 3 - IA West Smart Match CRM/scripts/run_react_e2e.py` — a standalone entry point that imports and runs the two test functions from `tests/test_react_e2e.py`. Follow the same pattern as `scripts/run_playwright_demo_qa.py`.

Structure:
```python
"""
Standalone runner for React E2E Playwright tests.

Usage:
    python scripts/run_react_e2e.py

Requires React at :5173 and FastAPI at :8000 to be running.
Start both: python start_fullstack.py
"""
from __future__ import annotations

import sys
import time
from pathlib import Path

# Add project root to sys.path so tests module is importable
ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))

from tests.test_react_e2e import test_qr_flow, test_feedback_flow


def main() -> None:
    results: list[dict[str, str]] = []

    for name, fn in [("qr_flow", test_qr_flow), ("feedback_flow", test_feedback_flow)]:
        print(f"\n{'='*60}")
        print(f"Running: {name}")
        print(f"{'='*60}")
        start = time.time()
        try:
            fn()
            elapsed = time.time() - start
            print(f"PASS: {name} ({elapsed:.1f}s)")
            results.append({"name": name, "status": "pass", "time": f"{elapsed:.1f}s"})
        except Exception as exc:
            elapsed = time.time() - start
            print(f"FAIL: {name} ({elapsed:.1f}s) -- {exc}")
            results.append({"name": name, "status": "fail", "time": f"{elapsed:.1f}s", "error": str(exc)})

    print(f"\n{'='*60}")
    print("Summary")
    print(f"{'='*60}")
    for r in results:
        status_marker = "PASS" if r["status"] == "pass" else "FAIL"
        print(f"  [{status_marker}] {r['name']} ({r['time']})")

    failed = [r for r in results if r["status"] != "pass"]
    if failed:
        print(f"\n{len(failed)} test(s) failed.")
        sys.exit(1)
    else:
        print("\nAll tests passed.")


if __name__ == "__main__":
    main()
```

**Part B: Run the tests to capture evidence screenshots**

After creating the runner, execute the tests. The React dev server and FastAPI backend must be running. The executor should:

1. Ensure Playwright browsers are installed: `python -m playwright install chromium`
2. Verify the servers are reachable: `curl -s -o /dev/null -w "%{http_code}" http://localhost:5173/` and `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/api/data/specialists`
3. Run: `cd "Category 3 - IA West Smart Match CRM" && python scripts/run_react_e2e.py`
4. Verify screenshots exist: `ls -la output/playwright/react-qr-flow.png output/playwright/react-feedback-flow.png`

If the servers are not running, the executor should start them first using `start_fullstack.py` or equivalent, wait for them to be ready, then run the tests.

If Playwright is not installed, run `pip install playwright && python -m playwright install chromium` first.

**Part C: Verify evidence files**

After tests pass, confirm both PNG files exist and have non-zero size. These files will be committed as evidence alongside the test scripts.
  </action>
  <verify>
    <automated>cd "Category 3 - IA West Smart Match CRM" && python -c "import ast; ast.parse(open('scripts/run_react_e2e.py').read()); print('syntax OK')" && test -s output/playwright/react-qr-flow.png && echo "QR screenshot exists" && test -s output/playwright/react-feedback-flow.png && echo "Feedback screenshot exists"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at "Category 3 - IA West Smart Match CRM/scripts/run_react_e2e.py"
    - grep "test_qr_flow" "Category 3 - IA West Smart Match CRM/scripts/run_react_e2e.py" returns a match
    - grep "test_feedback_flow" "Category 3 - IA West Smart Match CRM/scripts/run_react_e2e.py" returns a match
    - grep "__main__" "Category 3 - IA West Smart Match CRM/scripts/run_react_e2e.py" returns a match
    - File "Category 3 - IA West Smart Match CRM/output/playwright/react-qr-flow.png" exists and has size > 0
    - File "Category 3 - IA West Smart Match CRM/output/playwright/react-feedback-flow.png" exists and has size > 0
    - python scripts/run_react_e2e.py exits with code 0
  </acceptance_criteria>
  <done>run_react_e2e.py standalone runner exists, both tests pass, and both screenshot PNGs (react-qr-flow.png, react-feedback-flow.png) exist in output/playwright/ with non-zero file size, ready to be committed as evidence.</done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('tests/test_react_e2e.py').read())"` — valid Python syntax
2. `python -c "import ast; ast.parse(open('scripts/run_react_e2e.py').read())"` — valid Python syntax
3. `python scripts/run_react_e2e.py` exits 0 — both tests pass
4. `test -s output/playwright/react-qr-flow.png` — QR screenshot exists
5. `test -s output/playwright/react-feedback-flow.png` — Feedback screenshot exists
6. Both screenshots show meaningful content (not blank/error pages)
</verification>

<success_criteria>
- VERIFY-01 satisfied: Playwright test demonstrates QR code generation and scan attribution in the browser, with screenshot evidence
- VERIFY-02 satisfied: Playwright test demonstrates feedback submission and weight-shift analytics rendering in the browser, with screenshot evidence
- Both test artifacts (PNGs) committed as evidence alongside the test scripts
- Standalone runner (scripts/run_react_e2e.py) allows re-running tests with a single command
</success_criteria>

<output>
After completion, create `.planning/phases/15-build-quality-playwright-evidence/15-02-SUMMARY.md`
</output>
