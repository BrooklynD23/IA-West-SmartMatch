---
phase: 11-qr-code-generation-roi-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Category 3 - IA West Smart Match CRM/requirements.txt"
  - "Category 3 - IA West Smart Match CRM/src/api/main.py"
  - "Category 3 - IA West Smart Match CRM/src/api/routers/qr.py"
  - "Category 3 - IA West Smart Match CRM/src/qr/service.py"
  - "Category 3 - IA West Smart Match CRM/src/ui/data_helpers.py"
  - "Category 3 - IA West Smart Match CRM/tests/test_api_qr.py"
autonomous: true
requirements: []

must_haves:
  truths:
    - "The backend can deterministically generate a QR artifact for a speaker-event pair"
    - "A scan endpoint records the scan and redirects with referral metadata"
    - "Stats endpoints expose QR generation and scan counts for React analytics surfaces"
    - "All QR persistence remains local-file based and test-covered"
  artifacts:
    - path: "Category 3 - IA West Smart Match CRM/src/qr/service.py"
      provides: "QR generation, referral-code, and stats logic"
    - path: "Category 3 - IA West Smart Match CRM/src/api/routers/qr.py"
      provides: "FastAPI QR endpoints"
    - path: "Category 3 - IA West Smart Match CRM/tests/test_api_qr.py"
      provides: "QR API regression coverage"
  key_links:
    - from: "src/api/main.py"
      to: "src/api/routers/qr.py"
      via: "router mount"
      pattern: "qr"
    - from: "src/api/routers/qr.py"
      to: "src/qr/service.py"
      via: "service import"
      pattern: "src\\.qr\\.service"
---

<objective>
Build the backend contract for QR generation, scan tracking, and ROI statistics.

Purpose: create one deterministic backend workflow for referral-coded speaker-event QR assets and the analytics data React will later display.
Output: QR service module, FastAPI QR router, local storage support, and automated tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-qr-code-generation-roi-tracking/11-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add QR dependencies and a deterministic QR service module</name>
  <files>
    Category 3 - IA West Smart Match CRM/requirements.txt,
    Category 3 - IA West Smart Match CRM/src/qr/service.py,
    Category 3 - IA West Smart Match CRM/src/ui/data_helpers.py
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/requirements.txt,
    Category 3 - IA West Smart Match CRM/src/ui/data_helpers.py,
    Category 3 - IA West Smart Match CRM/src/outreach/pipeline_updater.py
  </read_first>
  <action>
    Add `qrcode` and `Pillow` to `requirements.txt`, then create `src/qr/service.py` with deterministic referral-code generation for speaker-event pairs, QR image creation, local manifest/log persistence, and stats helpers. Extend `data_helpers.py` with cache-aware read helpers for the QR manifest and scan log so the API layer can reuse established data-loading patterns.
  </action>
  <acceptance_criteria>
    - `requirements.txt` contains `qrcode`
    - `requirements.txt` contains `Pillow`
    - `src/qr/service.py` exists
    - `src/qr/service.py` contains a deterministic referral-code helper
    - `src/ui/data_helpers.py` contains QR-related loader helpers
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose QR generate/scan/stats endpoints through FastAPI</name>
  <files>
    Category 3 - IA West Smart Match CRM/src/api/main.py,
    Category 3 - IA West Smart Match CRM/src/api/routers/qr.py,
    Category 3 - IA West Smart Match CRM/tests/test_api_qr.py
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/src/api/main.py,
    Category 3 - IA West Smart Match CRM/src/api/routers/outreach.py,
    Category 3 - IA West Smart Match CRM/src/qr/service.py
  </read_first>
  <action>
    Create a new `qr.py` router mounted from `main.py` at `/api/qr` with three endpoints: one to generate/return a QR asset and metadata for a speaker-event pair, one to record a scan and redirect using the referral code, and one to return aggregate QR stats for analytics. Add focused API tests for deterministic code generation, redirect logging, and stats shape.
  </action>
  <acceptance_criteria>
    - `src/api/routers/qr.py` exists
    - `src/api/routers/qr.py` contains `/generate`
    - `src/api/routers/qr.py` contains `/scan/`
    - `src/api/routers/qr.py` contains `/stats`
    - `tests/test_api_qr.py` exists
    - `cd "Category 3 - IA West Smart Match CRM" && ./.venv/bin/python -m pytest tests/test_api_qr.py -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd "Category 3 - IA West Smart Match CRM" && ./.venv/bin/python -m pytest tests/test_api_qr.py -q
```
</verification>

