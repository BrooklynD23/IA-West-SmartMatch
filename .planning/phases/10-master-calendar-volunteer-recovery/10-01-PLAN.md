---
phase: 10-master-calendar-volunteer-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Category 3 - IA West Smart Match CRM/src/config.py"
  - "Category 3 - IA West Smart Match CRM/src/matching/factors.py"
  - "Category 3 - IA West Smart Match CRM/src/matching/engine.py"
  - "Category 3 - IA West Smart Match CRM/src/api/main.py"
  - "Category 3 - IA West Smart Match CRM/src/api/routers/calendar.py"
  - "Category 3 - IA West Smart Match CRM/tests/test_api_matching.py"
  - "Category 3 - IA West Smart Match CRM/tests/test_api_calendar.py"
autonomous: true
requirements: []

must_haves:
  truths:
    - "The matching engine exposes a canonical `volunteer_fatigue` factor and honors its weight"
    - "FastAPI exposes dedicated calendar endpoints for normalized event and assignment data"
    - "Recovery status can be derived from backend responses without React inventing its own private rules"
    - "Automated tests prove both the new factor and the new calendar routes"
  artifacts:
    - path: "Category 3 - IA West Smart Match CRM/src/config.py"
      provides: "Canonical factor registry including volunteer_fatigue"
    - path: "Category 3 - IA West Smart Match CRM/src/matching/factors.py"
      provides: "Fatigue scoring logic"
    - path: "Category 3 - IA West Smart Match CRM/src/api/routers/calendar.py"
      provides: "Calendar and assignment API contract"
    - path: "Category 3 - IA West Smart Match CRM/tests/test_api_calendar.py"
      provides: "Calendar API regression coverage"
  key_links:
    - from: "src/matching/engine.py"
      to: "src/config.py"
      via: "factor registry / weights"
      pattern: "volunteer_fatigue"
    - from: "src/api/main.py"
      to: "src/api/routers/calendar.py"
      via: "router mount"
      pattern: "calendar"
---

<objective>
Build the backend contract for the master calendar and recovery-period factor.

Purpose: make fatigue and assignment state first-class backend concepts so React can consume one authoritative calendar/recovery model instead of deriving conflicting logic client-side.
Output: canonical `volunteer_fatigue` scoring support plus new FastAPI calendar endpoints and automated tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-master-calendar-volunteer-recovery/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical volunteer_fatigue scoring support to the matching engine</name>
  <files>
    Category 3 - IA West Smart Match CRM/src/config.py,
    Category 3 - IA West Smart Match CRM/src/matching/factors.py,
    Category 3 - IA West Smart Match CRM/src/matching/engine.py,
    Category 3 - IA West Smart Match CRM/tests/test_api_matching.py
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/src/config.py,
    Category 3 - IA West Smart Match CRM/src/matching/factors.py,
    Category 3 - IA West Smart Match CRM/src/matching/engine.py,
    Category 3 - IA West Smart Match CRM/src/outreach/pipeline_updater.py,
    Category 3 - IA West Smart Match CRM/src/ui/data_helpers.py
  </read_first>
  <action>
    Extend the canonical factor registry in `config.py` with `volunteer_fatigue` at default weight `0.10`, then implement fatigue scoring in `factors.py` using recent assignment count, recency since last assignment, regional travel burden, and event cadence/duration proxies available in the current datasets. Update `compute_match_score()` and downstream rank/score responses so the new factor appears in both `factor_scores` and `weighted_factor_scores`. Add or extend tests to prove the factor is present and materially affects ranking when overrides change.
  </action>
  <acceptance_criteria>
    - `src/config.py` contains `volunteer_fatigue`
    - `src/matching/factors.py` contains a fatigue-scoring function
    - `src/matching/engine.py` contains `volunteer_fatigue` in the computed factor score payload
    - `tests/test_api_matching.py` contains at least one assertion covering `volunteer_fatigue`
    - `cd "Category 3 - IA West Smart Match CRM" && ./.venv/bin/python -m pytest tests/test_api_matching.py -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose normalized calendar events and assignment overlays through FastAPI</name>
  <files>
    Category 3 - IA West Smart Match CRM/src/api/main.py,
    Category 3 - IA West Smart Match CRM/src/api/routers/calendar.py,
    Category 3 - IA West Smart Match CRM/tests/test_api_calendar.py
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/src/api/main.py,
    Category 3 - IA West Smart Match CRM/src/api/routers/data.py,
    Category 3 - IA West Smart Match CRM/src/ui/data_helpers.py,
    Category 3 - IA West Smart Match CRM/data/pipeline_sample_data.csv,
    Category 3 - IA West Smart Match CRM/data/data_event_calendar.csv
  </read_first>
  <action>
    Create a dedicated `calendar.py` router mounted from `main.py` at `/api/calendar`. Add one endpoint that returns normalized event rows for the master calendar, including a coordinator-facing coverage status and color/status metadata, and a second endpoint that returns assignment overlays derived from pipeline rows. Keep the storage CSV-backed, use existing loader helpers, and write focused API tests that assert response shape and assignment/coverage fields.
  </action>
  <acceptance_criteria>
    - `src/api/routers/calendar.py` exists
    - `src/api/main.py` mounts a router with `/api/calendar`
    - `src/api/routers/calendar.py` contains `/events`
    - `src/api/routers/calendar.py` contains `/assignments`
    - `tests/test_api_calendar.py` exists
    - `cd "Category 3 - IA West Smart Match CRM" && ./.venv/bin/python -m pytest tests/test_api_calendar.py -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd "Category 3 - IA West Smart Match CRM" && ./.venv/bin/python -m pytest tests/test_api_matching.py tests/test_api_calendar.py -q
```
</verification>

