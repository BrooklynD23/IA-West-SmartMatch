---
phase: 12-continuous-algorithm-improvement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Category 3 - IA West Smart Match CRM/src/api/main.py"
  - "Category 3 - IA West Smart Match CRM/src/api/routers/feedback.py"
  - "Category 3 - IA West Smart Match CRM/src/feedback/acceptance.py"
  - "Category 3 - IA West Smart Match CRM/src/feedback/service.py"
  - "Category 3 - IA West Smart Match CRM/src/config.py"
  - "Category 3 - IA West Smart Match CRM/src/ui/data_helpers.py"
  - "Category 3 - IA West Smart Match CRM/tests/test_api_feedback.py"
autonomous: true
requirements: []

must_haves:
  truths:
    - "FastAPI exposes feedback submission and stats endpoints backed by shared service logic"
    - "Feedback records are persisted in a format React can consume, not only Streamlit session state"
    - "A bounded optimizer can produce updated weight suggestions or snapshots from recorded outcomes"
    - "Weight history is persisted as an auditable artifact"
  artifacts:
    - path: "Category 3 - IA West Smart Match CRM/src/feedback/service.py"
      provides: "Shared feedback persistence and optimization logic"
    - path: "Category 3 - IA West Smart Match CRM/src/api/routers/feedback.py"
      provides: "Feedback FastAPI contract"
    - path: "Category 3 - IA West Smart Match CRM/tests/test_api_feedback.py"
      provides: "Feedback API regression coverage"
  key_links:
    - from: "src/api/main.py"
      to: "src/api/routers/feedback.py"
      via: "router mount"
      pattern: "feedback"
    - from: "src/api/routers/feedback.py"
      to: "src/feedback/service.py"
      via: "service import"
      pattern: "src\\.feedback\\.service"
---

<objective>
Build the backend feedback and optimization contract for continuous algorithm improvement.

Purpose: replace the current Streamlit-only feedback island with a shared service that React and FastAPI can use, and generate auditable weight-improvement outputs from recorded outcomes.
Output: feedback service module, feedback API router, persisted weight history, and automated tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-continuous-algorithm-improvement/12-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared feedback persistence + optimization logic from the existing Streamlit model</name>
  <files>
    Category 3 - IA West Smart Match CRM/src/feedback/acceptance.py,
    Category 3 - IA West Smart Match CRM/src/feedback/service.py,
    Category 3 - IA West Smart Match CRM/src/config.py,
    Category 3 - IA West Smart Match CRM/src/ui/data_helpers.py
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/src/feedback/acceptance.py,
    Category 3 - IA West Smart Match CRM/src/config.py,
    Category 3 - IA West Smart Match CRM/src/ui/data_helpers.py
  </read_first>
  <action>
    Introduce `src/feedback/service.py` as the shared home for persisted feedback records, aggregate stats, and bounded weight-adjustment suggestions/history, reusing the existing acceptance/decline semantics from `acceptance.py` where possible. Refactor `acceptance.py` only as needed so Streamlit and FastAPI can share the same persistence and aggregation logic without duplicating business rules. Add cache-aware loader helpers for feedback logs and weight-history artifacts.
  </action>
  <acceptance_criteria>
    - `src/feedback/service.py` exists
    - `src/feedback/service.py` contains persisted feedback/stat logic
    - `src/feedback/service.py` contains weight-history or optimizer snapshot logic
    - `src/ui/data_helpers.py` contains feedback/weight-history loader helpers
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose feedback submission and optimization stats through FastAPI</name>
  <files>
    Category 3 - IA West Smart Match CRM/src/api/main.py,
    Category 3 - IA West Smart Match CRM/src/api/routers/feedback.py,
    Category 3 - IA West Smart Match CRM/tests/test_api_feedback.py
  </files>
  <read_first>
    Category 3 - IA West Smart Match CRM/src/api/main.py,
    Category 3 - IA West Smart Match CRM/src/api/routers/outreach.py,
    Category 3 - IA West Smart Match CRM/src/feedback/service.py
  </read_first>
  <action>
    Create a `feedback.py` router mounted from `main.py` at `/api/feedback` with endpoints for submitting structured feedback, retrieving aggregate stats, and retrieving current/historical weight recommendations. Add focused API tests covering persistence, stats shape, and optimizer output boundaries.
  </action>
  <acceptance_criteria>
    - `src/api/routers/feedback.py` exists
    - `src/api/routers/feedback.py` contains `/submit`
    - `src/api/routers/feedback.py` contains `/stats`
    - `tests/test_api_feedback.py` exists
    - `cd "Category 3 - IA West Smart Match CRM" && ./.venv/bin/python -m pytest tests/test_api_feedback.py -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd "Category 3 - IA West Smart Match CRM" && ./.venv/bin/python -m pytest tests/test_api_feedback.py -q
```
</verification>

