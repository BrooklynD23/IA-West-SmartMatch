# ── Core Framework ───────────────────────────────────────
streamlit==1.42.2
python-dotenv==1.0.1          # installed: 1.0.1

# ── LLM APIs ────────────────────────────────────────────
# Gemini API is accessed via the stdlib REST helper in src/gemini_client.py

# ── Data Processing ──────────────────────────────────────
pandas>=2.2.3                 # installed: 3.0.1 (pinned version unavailable on this platform)
numpy==1.26.4                 # installed: 1.26.4

# ── Visualization ────────────────────────────────────────
plotly==5.24.1                # installed: 5.24.1

# ── Web Scraping (Sprint 2, install now) ─────────────────
beautifulsoup4==4.12.3
requests==2.32.3
playwright==1.58.0            # pyee>=13 aligns with streamlit-webrtc>=0.64

# ── QR / Imaging ────────────────────────────────────────
qrcode==8.2
Pillow==11.3.0

# ── Matching Utilities ───────────────────────────────────
scipy==1.14.1                 # installed: 1.14.1
scikit-learn>=1.5.2           # installed: 1.8.0 (pinned version unavailable on this platform)
fuzzywuzzy>=0.18.0            # fuzzy role matching in matching/factors.py
python-Levenshtein>=0.12.0    # accelerates fuzzywuzzy (optional but recommended)

# ── Dev / Testing ────────────────────────────────────────
pytest==8.3.4                 # installed: 8.3.4
pytest-cov>=6.0.0             # installed: 7.0.0

# ── REST API ──────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# ── Voice I/O (Phase 4 / Jarvis Command Center) ────────────
# KittenTTS 0.8.1 wheel declares Requires-Python <3.13; on 3.13+ use Python 3.11/3.12 venv for TTS or rely on text-only Jarvis.
https://github.com/KittenML/KittenTTS/releases/download/0.8.1/kittentts-0.8.1-py3-none-any.whl ; python_version < "3.13"
faster-whisper>=1.0.0
streamlit-webrtc==0.64.5      # 0.6.x breaks on Streamlit 1.42+ (removed report_thread)
soundfile
